{
  "name": "tanstack-federation",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.33.1",
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev --filter=@repo/host --filter=@repo/remote-csr --filter=@repo/remote-ssr",
    "preview": "turbo preview --filter=@repo/host --filter=@repo/remote-csr --filter=@repo/remote-ssr",
    "test": "turbo test",
    "typecheck": "turbo typecheck"
  },
  "devDependencies": {
    "turbo": "^2.9.16"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild",
      "lightningcss"
    ]
  }
}
